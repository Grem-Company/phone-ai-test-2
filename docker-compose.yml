services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "7860:7860"
    restart: unless-stopped
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
